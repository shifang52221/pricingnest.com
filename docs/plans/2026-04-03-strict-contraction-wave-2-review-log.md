# Strict Contraction Wave 2 Review Log

## What This Batch Changed
- Moved another `10` high-confidence ledger candidates from `index,follow` to `noindex,follow`.
- Guide contraction in this batch:
  - `pricing-migration-plan`
  - `pricing-page-comparison-table`
  - `pricing-segmentation`
  - `rfp-pricing-response`
  - `seat-utilization-forecast`
  - `usage-mix-modeling`
- Glossary contraction in this batch:
  - `backup`
  - `burst-traffic`
  - `replication`
  - `usage-forecast`
- Tightened the `/guides/` and `/glossary/` hubs so they surface retained-core pages rather than `move-to-noindex` or `observe-for-now` inventory pages.

## Why These Pages Were Chosen
- Every selected page was already marked `move-to-noindex` in the indexed-content review ledger.
- The selected pages are among the thinnest still-indexable support pages and lack meaningful trust signals such as `reviewed`, `reviewedBy`, or `sources`.
- Two of the selected pages were still receiving hub-level exposure despite already being identified as later-wave contraction candidates:
  - `pricing-page-comparison-table`
  - `usage-forecast`

## What This Batch Did Not Change
- No calculator formulas changed.
- No URLs changed.
- No retained-core page was rewritten or expanded.
- No broad redesign work was started.

## Remaining Risks
- Many `observe-for-now` pages are still thin and still lack review metadata or source blocks.
- Some non-core tool pathways still link to weaker support guides, even though the main retained calculator clusters are already tighter.
- Several retained glossary pages remain indexed but still need strengthening before they fully justify their index slots.

## Recommended Next Batch
- Strengthen one retained guide batch instead of opening another broad contraction wave immediately.
- Best next candidates:
  - `value-metric-selection`
  - `minimum-commitment-model`
  - `price-per-gb-month-explained`
  - `rate-limit`
  - `billing-cycle`
