# Retained Core Strengthening Queue

## Queue Rules
- Only strengthen pages that still sit inside the retained indexed surface.
- Guide work comes before glossary work.
- Glossary work only serves retained guides and core calculator clusters.
- Do not reopen pages already placed in `move-to-noindex` unless search evidence proves a real standalone intent.
- Do not start another broad content wave before the current unified batch is live-verified.

## Immediate Guide Queue
1. `value-metric-selection`
   - Highest-value bridge between the usage-based and API pricing calculator clusters.
   - Next strengthening pass should sharpen intent, decision framing, and evidence-backed next steps.
2. `minimum-commitment-model`
   - Cross-cluster page supporting usage, compute, and annual discount decisions.
   - Next pass should tighten commitment thresholds, commit-vs-platform-fee trade-offs, and linked tool pathways.
3. `saas-gross-margin-targets`
   - Shared gross-margin guardrail page used by core pricing decisions.
   - Next pass should keep it finance-credible and more tightly connected to calculator interpretation.
4. `price-per-gb-month-explained`
   - Strongest storage-adjacent retained explainer that is still indexed.
   - Next pass should align it even more closely to `storage-cost-calculator` interpretation and request/retrieval splits.
5. `pricing-page-trust-elements`
   - Highest-value retained UX/SEO trust page.
   - Next pass should reduce template feel and keep the page focused on buyer confidence signals that matter on real pricing pages.

## Secondary Guide Queue
1. `api-pricing-model`
   - Direct API pricing-intent page that should become stronger than the checklist-style API support inventory around it.
2. `bandwidth-pricing-guide`
   - Retained bandwidth decision page that should be tightened after the unified batch lands and the new bandwidth calculator copy is live.
3. `storage-retrieval-fees`
   - Useful retained storage-support page if storage cluster evidence still looks thin after the next tool batch.
4. `annual-prepay-discount`
   - Already stronger than most retained guides; only reopen if annual discount cluster still lacks a clear decision page after query concentration shifts.

## Existing Retained Guides To Leave Stable For Now
- `api-cost-estimation`
- `compute-cost-modeling`
- `storage-costs-and-pricing`
- `usage-based-pricing-examples`

These already have stronger depth and trust coverage. Reopen them only for targeted fixes, not broad rewrites.

## Immediate Glossary Queue
1. `rate-limit`
   - Best next glossary support page for the API pricing cluster.
2. `annual-prepay-discount`
   - Keeps annual discount terminology aligned with the retained annual pricing guide and calculator.
3. `usage-based-pricing`
   - Central retained term supporting the usage-based calculator and related guide layer.
4. `churn`
   - High-value decision-support term for pricing changes, annual commitments, and retention trade-offs.

## Secondary Glossary Queue
1. `api-call`
2. `bandwidth`
3. `egress`
4. `billing-cycle`

These are the next glossary terms most directly connected to retained tool clusters after the immediate queue is complete.

## Glossary Terms Already Above The Current Floor
- `value-metric`
- `pricing-metric`
- `unit-cost`
- `gross-margin`
- `included-usage`
- `overage`
- `minimum-commitment`
- `gb-month`

These remain part of the retained glossary layer, but they should only be revisited if a retained guide or tool still needs stronger interpretation support.

## Remaining Core Tool Tightening Order
1. Current unified batch: `bandwidth-cost-calculator` tightening plus the first indexed-content governance wave.
2. Next calculator candidate after live verification: `storage-cost-calculator`.
3. Only after storage is live-verified should we decide whether another tool-intent batch is necessary.

## Stop Conditions
- Do not start a new queue item until the current unified batch is deployed and checked with no-cache HTML verification.
- Do not expand the queue with new content types or new URLs.
- Do not promote an `observe-for-now` page into the queue without a fresh ledger review.
